<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :frase %><br>
    <%= f.text_area :frase %>
  </div>
  <div class="field">
    <%= f.label :data_publicacao %><br>
    <%= f.date_select :data_publicacao %>
  </div>
  <div class="field">
    <%= f.label :data_dilma %><br>
    <%= f.date_select :data_dilma %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
